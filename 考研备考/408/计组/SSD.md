应用程序 read()
        ↓
文件系统找到对应逻辑块
        ↓
设备独立性软件形成块读请求
        ↓
SSD 驱动程序生成 NVMe/SATA 读命令
        ↓
SSD 控制器收到命令：读 LBA 10000
        ↓
FTL 查询映射表：LBA 10000 在物理页 P
        ↓
从 NAND Flash 读出该物理页
        ↓
通过 DMA 把数据送入内存
        ↓
驱动程序收到完成通知
        ↓
数据返回给上层
