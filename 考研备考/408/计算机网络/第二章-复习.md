
信源（源头）
信宿（归宿）
信道是信号的载体，信号是数据的载体

信号划分为：模拟信道，数字信道；无线信道，有线信道
信号有 基带信号（原始，未经过调制） 宽带信号（经过调制）
传输方式：并行（短距离），串行（长距离）

同步传输（内同步（将时钟编码到数据内），外同步（增加时钟信号线）），异步传输
（使用起始位，终止位）


- 在题目中同时出现**信噪比**，**码元**的相关信息的时候，注意使用两种方式进行约束
- 二进制信号-->指的是一个码元使用1个bit位表示
一个码元携带能多种类的信息是有代价的，需要加强信号的功率，对信道的要求更高

一个码元携带4种信号--->称为 四进制码元--->2bits
一个码元携带8种信号--->称为 八进制码元--->3bits

 信源发出信号后一般需要**经过变换器转换为**适合在信道上传输的信号
 经过 **反变换器** 还原为原始信息

在现代计算机中外设改用高速串行总线
但是CPU与主存仍是并行

反向非归零编码-跳变表示数据，跳变本身可用于辅助时钟恢复，兼顾NRZ效率，与一定自同步
增加冗余位，可支持自同步

DPSK是一种**相对调相方式**，通过检测当前码元与前一个码元的载波相位差来传输数字信息

以太网使用曼彻斯特编码
令牌环网使用差分曼彻斯特编码

使用全反射-纤芯的n(折射率)比包层大


命令规则
10Base5-->10表示传输速率，Base表示传输数字信号5表示最远传输距离500m
10Base2-->
10BaseF* 光纤
10BaseT* 双绞线

无线电波（波长长）
短波（波长短）传输距离短，需要中继


![[Pasted image 20260531152421.png]]



加深理解，物理层对等实体之间的传输

 不会“直接把同轴电缆上的电压高低电平原样接到双绞线上”，所以一般不会因为电压标准不同而把 0/1 弄错。

关键在于：
Hub 不是一根简单的转接线，而是物理层中继设备

---

## 1. Hub 会重新生成信号

在这种连接中：

```text
主机 A --- 同轴电缆 --- Hub --- 双绞线 --- 主机 B
```

Hub 收到同轴电缆上传来的信号后，不是把电压直接导通到双绞线端口，而是：

1. 在同轴接口电路中识别比特流；
2. 恢复出 0/1 数据和时钟；
3. 再按照双绞线接口的电气规范重新发送出去。

也就是说：

\[
\text{同轴电气信号}
\rightarrow
\text{Hub 识别为比特}
\rightarrow
\text{重新编码成双绞线电气信号}
\]

因此只要 Hub 支持这两种接口，它内部就有相应的物理层收发电路来完成转换。

---

## 2. 同轴和双绞线的电气标准确实不同

例如早期以太网：

### 同轴电缆

如 10BASE2、10BASE5，使用同轴电缆，总线型结构。

它的信号电气特性、阻抗、连接方式都和双绞线不同。

---

### 双绞线

如 10BASE-T，使用 RJ-45 接口，星型结构。

它使用双绞线上的差分信号，电气特性也不同。

---

所以如果你只是用一个被动转接头，想把同轴电缆直接接到双绞线线上：

\[
\boxed{\text{通常不可以}}
\]

因为电气标准不兼容，阻抗、编码、收发方式都不同。

---

## 3. 为什么用 Hub 可以？

因为带 BNC 口和 RJ-45 口的 Hub 内部相当于有不同的 PHY 接口。

可以理解为：

```text
同轴口 PHY  ←→  Hub 中继逻辑  ←→  双绞线口 PHY
```

同轴口 PHY 负责理解同轴电缆上的以太网信号。

双绞线口 PHY 负责按照双绞线标准发出信号。

因此 Hub 做的是“物理层再生/转换”，而不是“电线直连”。

---

## 4. 会不会弄错高低电平？

正常不会。

因为对以太网来说，接收端并不是简单看“电压高就是 1，电压低就是 0”。

以太网物理层通常还涉及：

- 编码方式；
- 时钟恢复；
- 差分信号；
- 曼彻斯特编码；
- 信号整形；
- 碰撞检测；
- 链路完整性检测。

Hub 的对应接口会按标准解析信号。

所以只要设备符合标准，就不会把 0/1 弄错。

---

## 5. 一个类比

可以把 Hub 想成翻译员：

```text
同轴电缆语言 → Hub 听懂 → 翻译成双绞线语言
```

而不是：

```text
同轴电缆电压 → 直接接到双绞线
```

如果直接接，就像让两个说不同语言的人硬接电话线，当然不行。

但有 Hub 作为翻译，就可以通信。

---

## 6. 结论

你的担心是对的：

同轴电缆和双绞线的电气标准确实不同，不能直接硬接。

但如果使用的是支持两种介质的 Hub：

Hub 会在物理层识别并重新生成信号，因此不会简单地把高低电平弄错。

所以：

```text
主机 A --- 同轴电缆 --- Hub --- 双绞线 --- 主机 B
```

可以正常通信的前提是：

1. Hub 有对应的同轴接口和双绞线接口；
2. 两边以太网速率和标准兼容；
3. 线缆、终端电阻、接口连接符合规范。




